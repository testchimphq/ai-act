# Anthropic Claude Provider

Configure the Anthropic Claude provider by setting:

- `CLAUDE_API_KEY` – required
- `CLAUDE_MODEL` – optional (defaults to `claude-3-sonnet-20240229`)
- `CLAUDE_MAX_TOKENS` – optional (defaults to `1024`)

Screenshots are transmitted as base64 image parts when present.
