# Google Gemini Provider

Configure the Google Gemini provider by setting:

- `GEMINI_API_KEY` – required
- `GEMINI_MODEL` – optional (defaults to `gemini-1.5-flash`)

The provider sends the system prompt, user prompt, and optional screenshot to Gemini using the Google Generative AI SDK.
